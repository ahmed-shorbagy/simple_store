import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryColor = Color(0xFF6200EE);
  static const Color secondaryColor = Color(0xFF03DAC6);
  static const Color errorColor = Color(0xFFB00020);
  static const Color backgroundColor = Colors.white;
  static const Color textColor = Colors.black87;
  static const Color subtitleColor = Colors.black54;

  // Cart colors
  static const Color cartIconColor = primaryColor;
  static const Color cartBadgeColor = errorColor;

  // Auth colors
  static const Color authButtonColor = primaryColor;
  static const Color authInputBorderColor = Colors.grey;
}
